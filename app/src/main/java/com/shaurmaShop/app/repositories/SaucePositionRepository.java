package com.shaurmaShop.app.repositories;

import com.shaurmaShop.app.models.SaucePosition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SaucePositionRepository extends JpaRepository<SaucePosition, Long> {
}