package com.shaurmaShop.app.repositories;

import com.shaurmaShop.app.models.ShawarmaPosition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ShawarmaPositionRepository extends JpaRepository<ShawarmaPosition, Long> {
}